import React from 'react';

function Opportunities() {
    return(
    //html goes here
    // this will primarily exist as a container for the actual cards which will be a separate component that we
    // map into this section so we can only create 1 card and then fill it with the necessary information in a 
    // forEach type of function (REACT only works with map, so we'll 'map' the information onto each card from a 
    // separate file with the information in Json format)
    )
}

export default Opportunities;